
#Prevents the server from displaying the file list.
Options -Indexes 

#Activate the link rewriting engine
RewriteEngine On

RewriteBase /7X_PHARMA_CORE/public/

RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

RewriteRule ^(.*)$ index.php?url=$1 [QSA,L]